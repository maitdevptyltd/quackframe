[project]
name = "quackframe-secrets-example"
version = "0.0.0"
dependencies = ["quackframe[prefect]"]

[tool.quackframe]
runtime = "direct"

[tool.quackframe.database]
mode = "memory"

[tool.quackframe.functions]
enabled = ["register_secret"]
