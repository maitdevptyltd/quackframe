{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Quackframe: Run current SQL",
      "type": "node-terminal",
      "request": "launch",
      "command": "quackframe run \"${file}\"",
      "cwd": "${workspaceFolder}"
    }
  ]
}
